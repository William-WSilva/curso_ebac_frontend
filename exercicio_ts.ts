const multiplicacao = (num1: number, num2:number): number => num1 * num2;
const saudacao = (nome: string): string => `Olá ${nome}`;

console.log(multiplicacao(5,100));
console.log(saudacao("William Silva"));